
// Assignment Code
var generateBtn = document.querySelector("#generate");

// Write password to the #password input
function writePassword() {
  var password = generatePassword();
  var passwordText = document.querySelector("#password");
  passwordText.value = password;
}

generateBtn.addEventListener("click", writePassword);
 
function figenerateTimer(secs) {


}

function generateRndmInt(start,end) {
  RndmInt = Math.floor(Math.random()*(end-start+1))+start;
}

function generateQuestion() {
pickedNo = generateRndmInt(1,questions.length);
question = questions.find(({questionNo}) => questionNo===RndmInt);
questionText = questions.question.value;
choiceList = questions.find(({questionNo}) => questionNo === pickedNo);
return questionText, choiceList;
  }

generateQuestion();
console.log(questionText, choiceList);

questions[{
  questionNo: 1,
  question: "Objects are defined in Javascript using what character?",
  choice: function getChoices(questionNo){choices.filter(choices.questionNo)===questions.questionNo}
  }
  // {questionNo: 2,
  // question: "What command allows you to use a variable generated by a function?",
  // choice:[choices]},
  // {questionNo: 3,
  // question: "What symbol is a wildcard in Javascript?",
  // choice:[Choices]}
]
  
choices[{
  questionNo: 1,
  choiceNo: 1,
  choice: "[",
  correct: false
  },
  {questionNo:1,
  choiceNo: 2,
  choice: "(",
  correct: false},
  {questionNo: 1,
  choiceNo: 3,
  choice: "{",
  correct: true}]

  // questions.splice(questionNo, 1)
  // questions.find(questions.questionNo === rndmPos)